# NOTE: Keep this file ASCII-only. Japanese Windows pip reads it as CP932
#       and fails on UTF-8 comments. See README for explanations in Japanese.

# --- Core (required) ---
PyYAML>=6.0
Flask>=3.0
requests>=2.31

# --- Optional features ---
# Handover prediction / theoretical SINR (TLE + SGP4)
skyfield>=1.48
numpy>=1.24

# Adapter names in the network info panel (multi-NIC)
psutil>=5.9

# Strict HTML parsing for Kymeta/Intellian scraping (regex fallback works without)
beautifulsoup4>=4.12

# Only if the Intellian terminal provides SNMP
pysnmp>=4.4

# NOTE: The Starlink collector uses the external `grpcurl` binary
#       (https://github.com/fullstorydev/grpcurl). Install it on the OS,
#       not via pip. See README "Starlink Mini" section.
